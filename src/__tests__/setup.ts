/**
 * Vitest Setup File
 * Global test configuration and setup
 */

// Setup for node environment (pure TypeScript/JavaScript tests)
// For DOM tests, use project-specific configuration with jsdom environment
